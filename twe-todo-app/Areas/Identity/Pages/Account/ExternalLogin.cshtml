@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "会員登録";
}

<h1>@ViewData["Title"]</h1>
<h4>@Model.LoginProvider を関連付けます。</h4>
<hr />

<p class="text-info">
    <strong>@Model.LoginProvider</strong>での認証に成功しました。<br>
    登録するTwitterアカウントに相違なければ登録を完了してください。<br>
    完了することで「3分間本気出す。」に会員登録されます。
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <div class="input-group">
                    <div class="input-group-prepend">
                        <div class="input-group-text">Twitter ID @@</div>
                    </div>
                    <input asp-for="Input.TwitterName" class="form-control" readonly />
                </div>
            </div>
            <a class="btn btn-primary" href="/" role="button">戻る</a>
            <button type="submit" class="btn btn-primary">完了</button>
        </form>
    </div>
</div>